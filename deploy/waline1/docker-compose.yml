services:
  waline:
    image: lizheming/waline:latest
    container_name: waline1
    restart: always
    env_file:
      - .env
    ports:
      - "127.0.0.1:8361:8360"
    volumes:
      - ./data:/app/data
