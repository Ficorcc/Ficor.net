---
import AstroComments from './AstroComments.astro';
const { title = '', path = Astro.url.pathname, commentSystem = 'fiscus' } = Astro.props;
---
{commentSystem !== 'none' && <AstroComments pageId={path} pageTitle={title} />}
